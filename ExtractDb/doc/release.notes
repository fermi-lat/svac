v0

first version of code to extract housekeeping data from the database and store 
them in a root file. 

first version of root macro to plot physics data against housekeeping data

v0r1

modify code so that it can read in time in a string format rather than UTC